#include"player.h"

void printPlayer(NODE * group[], int);
void insertList(NODE * group[], NODE * temp);
void removeList(NODE * group[], int i, NODE * count);
void exchangeList(NODE * x, NODE *y);
void savePlayer(NODE * group[]);
void plush(NODE * group[]);
